- url_list = url_for(controller: type_model, action: 'index')
- url_new = url_for(controller: type_model, action: 'new')
- if %w(users).include?(type_model)
  - url_migracion = url_for(controller: type_model, action: 'dbf')

.container-fluid
  .row
    .col-sm-3.col-md-2.sidebar.menu_left
      %ul.nav.nav-sidebar
        %li.active
          = link_to t("#{type_model}.title.list"), url_list
        %li
          = link_to t("#{type_model}.title.new"), url_new
        - if %w(users).include?(type_model)
          %li
            = link_to 'Migración de Usuarios', url_migration
