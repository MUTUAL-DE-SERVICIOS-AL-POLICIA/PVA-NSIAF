%h1 Listing proceedings

%table
  %tr
    %th User
    %th Admin
    %th Proceeding type
    %th
    %th
    %th

  - @proceedings.each do |proceeding|
    %tr
      %td= proceeding.user
      %td= proceeding.admin
      %td= proceeding.proceeding_type
      %td= link_to 'Show', proceeding
      %td= link_to 'Edit', edit_proceeding_path(proceeding)
      %td= link_to 'Destroy', proceeding, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Proceeding', new_proceeding_path
