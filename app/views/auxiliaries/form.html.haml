.page-header
  %h1= t("auxiliaries.title.#{params[:action]}")

.col-md-8.column.col-md-offset-1
  = simple_form_for @auxiliary, html: { class: 'form-horizontal' } do |f|
    = f.input :code
    = f.input :name
    = f.association :account, include_blank: false

    = render 'shared/form_buttons'
