.col-md-8.column.col-md-offset-1
  = simple_form_for @entity, html: { class: 'form-horizontal' } do |f|
    = f.input :code
    = f.input :name
    = f.input :acronym
    .form-group
      .col-lg-offset-5.col-lg-9
        = button_tag class: 'btn btn-primary' do
          %span.glyphicon.glyphicon-ok
          Aceptar
        = link_to entities_path, class: 'btn btn-danger' do
          %span.glyphicon.glyphicon-remove
          Cancelar
