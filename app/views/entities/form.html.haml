.page-header
  %h1= t("entities.title.#{params[:action]}")

.col-md-8.column.col-md-offset-1
  = simple_form_for @entity, html: { class: 'form-horizontal' } do |f|
    = f.input :code
    = f.input :name
    = f.input :acronym

    = render 'shared/form_buttons'
